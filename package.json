{
  "name": "college-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install:all && cd backend && cp .env.example .env && echo '\\n✅ Done! Edit backend/.env with your DATABASE_URL, then run: cd backend && npm run db:migrate && npm run db:seed'"
  }
}
